import React from "react";
import { Stack } from "@fluentui/react";
import { BR, Title } from "../../utils";

const MultipleScripts = () => {
  return (
    <Stack>
      <Title title={"using multiplescripts and colision"} />
      <Stack>(insert link to new scratch project or teach how to import)</Stack>
      <Stack>
        "touching color ()" and "touching "sprite?" " is a block that detect is
        the sprite touching another sprite. We can use this to detect colision
        with another sprite and decide what action to do.
      </Stack>
      <BR />
    </Stack>
  );
};

export { MultipleScripts };
